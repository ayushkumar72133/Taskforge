[variables]
NIXPACKS_APP_DIR = "."

providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node backend/dist/index.js"
