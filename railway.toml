[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm run build --workspace=backend && npm run build --workspace=frontend"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3